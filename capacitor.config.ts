import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'pagination-task',
  webDir: 'dist/pagination-task',
  bundledWebRuntime: false
};

export default config;
